{
  "name": "@rsl/harness-fake",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic fake harness for CI testing without real model calls",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": "./dist/index.js" },
  "scripts": { "build": "tsc", "clean": "rm -rf dist", "lint": "echo 'lint ok'", "typecheck": "tsc --noEmit", "test": "echo 'test ok'" },
  "dependencies": { "@rsl/harness-core": "workspace:*" },
  "devDependencies": { "typescript": "^5.7.0", "vitest": "^3.0.0" }
}
