{
  "name": "@rsl/policies",
  "version": "0.1.0",
  "private": true,
  "description": "Policy enforcement for Regenerable Software Lab — dependency allowlists, network policy, filesystem policy",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "echo 'lint ok'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'test ok'"
  },
  "dependencies": {
    "@rsl/benchmark-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
