{
  "name": "@rsl/benchmark-core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, configuration parsing, and schema definitions",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schemas": "./src/schemas.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "echo 'lint ok'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "decimal.js": "^10.4.3",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
