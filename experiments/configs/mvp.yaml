# MVP Experiment Manifest — Regenerable Software Lab
# SPEC.md §28 — Experiment Manifest
#
# Full matrix: 2 profiles x 2 models x 2 harnesses x 3 seeds = 24 runs

id: mvp-001
benchmark: order-pricing
benchmark_version: 0.1.0

profiles:
  - basic
  - behavioral

models:
  - provider: openai
    model: gpt-5.6
  - provider: anthropic
    model: claude-sonnet

harnesses:
  - codex
  - generic-cli

seeds:
  - 11
  - 22
  - 33

limits:
  wall_clock_seconds: 1800
  max_cost_usd: 10
